{
  "scripts": {
    "build:rust": "cargo run --manifest-path statics/Cargo.toml",
    "build:ts": "tsc",
    "build": "npm run build:rust && npm run build:ts",
    "postbuild": "xcopy public build /E /Y /I"
  }
}